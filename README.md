# Matrix_Exercise
Use **Integer matrix** and add neighbor checks
Change **matrix** type from *int[][] to Integer[][]* and switch loop counters to *Integer*. Fix inner loop bound to use *mat[i].length*. Replace unsafe left-value check with explicit boundary checks and add prints for **Right**, **Up** and **Down** neighbors with appropriate index guards to avoid out-of-bounds access and to report adjacent values when the target element is found.
